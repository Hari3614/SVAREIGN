import 'package:flutter/cupertino.dart';

class Upyogikaledeletechytho extends StatelessWidget {
  const Upyogikaledeletechytho({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
